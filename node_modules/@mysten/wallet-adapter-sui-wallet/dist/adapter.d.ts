import { SignableTransaction, SuiTransactionResponse } from "@mysten/sui.js";
import { WalletAdapter } from "@mysten/wallet-adapter-base";
/**
 * @deprecated This wallet adapter has been replaced by the `WalletStandardAdapterProvider`.
 */
export declare class SuiWalletAdapter implements WalletAdapter {
    connecting: boolean;
    connected: boolean;
    getAccounts(): Promise<string[]>;
    signAndExecuteTransaction(transaction: SignableTransaction): Promise<SuiTransactionResponse>;
    name: string;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    constructor();
}
